#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../huddle/bin/huddle" "$@"
  ret=$?
else 
  node  "$basedir/../huddle/bin/huddle" "$@"
  ret=$?
fi
exit $ret
