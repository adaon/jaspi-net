var jaspi = jaspi || {};
jaspi.templates = {};

(function (exports) {
    
    exports.get = function (name, callback) {
        var app = location.pathname;
        $.get(app + 'templates/' + name + '.html', function (content) {
            
        });
    };
    
}(jaspi.templates));